stages:
  - test

unit-test:
  stage: test
  image: python:3.11
  before_script:
    - python -m pip install --upgrade pip
    - python -m pip install -e .
    - python -m pip install pytest
  script:
    - python -m pytest
