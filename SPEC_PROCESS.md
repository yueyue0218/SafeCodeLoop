# SafeCodeLoop 规约形成与演化过程（SPEC_PROCESS）

> 项目类型：A · Coding Agent Harness
> 过程周期：2026-08-09—2026-08-14
> 对应规约：`SPEC.md`
> 对应计划：`PLAN.md`
> 过程证据：`AGENT_LOG.md`、Git 提交与 PR、`tests/`、`demos/`、CI 和 Docker 验证记录

## 1. 文档目的

本文档不重复介绍 SafeCodeLoop 的功能，而是回答四个过程性问题：

1. 项目规约为什么被写成现在这样；
2. 哪些关键设计来自 AI 建议，哪些由项目负责人判断、修改或否决；
3. 实现、测试和外部验证如何反过来暴露规约缺陷；
4. 如何证明最终系统不是一次性生成，而是经过“提出假设—构造反例—修正规约—TDD 实现—回归验证”的连续演化。

本文档采用“决策—证据—影响”的记录方式。过程中的测试数量、提交哈希、分支和运行结果均可在仓库中复核；少量早期讨论内容根据当时的文档与实现结果进行结构化复原，目的在于解释决策因果，而不是制造无法验证的开发活动。

### 1.1 与课程评分要求的对应关系

| 过程文档要求 | 本文对应位置 |
|---|---|
| brainstorming 关键节点与智能体追问 | 第 3 节 |
| 至少 3 轮关键迭代的对话节选与处理决策 | 第 3.3—3.7 节，共 5 轮 |
| AI 建议的采纳、修改与推翻 | 第 3 节、第 16 节 |
| brainstorming 技能的优点与不满 | 第 20 节 |
| spec/plan 如何从讨论变成可执行文档 | 第 4—12 节 |
| 过程真实性与可核验证据 | 第 14、17、18 节 |

## 2. 过程方法：规约不是前置作文，而是可证伪假设

SafeCodeLoop 没有把 `SPEC.md` 当作实现前一次性写完的静态说明，而是把其中每一条机制要求视为一个需要被测试证伪的工程假设。

基本循环如下：

```text
课程要求 / 用户目标
        ↓
形成可执行的 SPEC 条款
        ↓
拆成 PLAN 中的最小任务与验收标准
        ↓
先编写能暴露缺口的失败测试
        ↓
实现最小机制并进行全量回归
        ↓
用 demo、冷启动 agent、真实 provider 或容器环境做外部验证
        ↓
发现“规约写了但语义不完整”或“实现通过但机制可绕过”
        ↓
修订 SPEC / PLAN / 测试，再进入下一轮
```

这个过程有三条约束：

- 机制必须在移除真实 LLM 后仍可确定性测试，不能把行为隐藏在提示词中；
- 文档中的“已完成”必须对应代码、测试、日志、提交或可复现命令中的至少一种证据；
- AI 可以提出方案和生成实现，但范围裁剪、安全边界、交付取舍和最终验收由项目负责人决定。

## 3. 与 Superpowers brainstorming 的协作记录

### 3.1 brainstorming 的实际使用方式

我没有直接要求 AI “生成一份 SPEC 和 PLAN”，而是先把课程原始要求、A 类项目边界、我能投入的时间和交付限制放进 brainstorming。智能体每轮只追问会改变设计方向的问题；我回答或提供新的课程信息后，再让它总结当前共识、尚未决定项和可能的验收证据。

对话采用了以下节奏：

```text
给出原始目标
→ 智能体一次追问一个高影响问题
→ 我说明偏好、时间或课程约束
→ 智能体提出 2—3 个可比较方案
→ 我选择、否决或组合方案并说明原因
→ 智能体回写 SPEC 条款与 PLAN 任务
→ 我用冷启动验证和后续测试检查条款是否足够明确
```

下面的对话节选依据早期讨论、`SPEC.md`/`PLAN.md` 修订痕迹和 `AGENT_LOG.md` 整理。为提高可读性，删除了重复确认和工具输出，并把同一主题的连续短消息合并；它们表达的是当时真实的决策内容，不声称是逐字聊天导出。

### 3.2 智能体追问中最有价值的问题

| 智能体追问 | 为什么是好问题 | 它改变了什么 |
|---|---|---|
| “你要交的是使用现成 agent 的应用，还是自己实现 agent 外围控制系统？” | 先消除项目类别误读 | 项目从普通应用收敛为 Coding Agent Harness |
| “如果拿掉真实 LLM，哪些机制仍能被单元测试证明？” | 把课程要求转换成可证伪标准 | 确立 MockLLM 和确定性测试优先 |
| “六个维度都平均实现，还是选择一个机制密集方向做深？” | 迫使我处理广度与深度冲突 | 六维最小覆盖，治理 + 反馈做深 |
| “危险操作是全部禁止，还是存在合理但需要人确认的操作？” | 把安全从二元规则提升为治理模型 | allow/block/needs_approval 三态设计 |
| “测试失败后，什么条件下才允许 agent 声明完成？” | 识别出仅回灌反馈仍可能假成功 | 后续形成完成门槛、预算和熔断器 |
| “评审者在没有 key、没有网络的新机器上如何复现核心贡献？” | 将实现问题连接到交付证据 | Mock demo、CLI、CI、Docker、release 路线 |

### 3.3 关键迭代一：先决定“做什么”，而不是先选技术栈

**初始设想：** 做一个能聊天并帮助修改代码的 AI 应用，可能带一个简单网页。

**整理后的对话节选：**

> **智能体：** 课程把你归到 A 类 Agent Harness 后，评分对象可能不是聊天界面。你希望证明“模型会写代码”，还是证明“模型的动作如何被解析、执行、阻止和纠正”？
> **我：** 我更想做课程要求最匹配、能稳妥验收的方案，但不想做一个巨大系统。
> **智能体：** 那么先把模型能力视为外部依赖。项目本体至少要包含 loop、action、tools、memory、guardrail、feedback 和 config。你是否接受“拿掉真实模型，核心机制仍然通过测试”作为设计红线？
> **我：** 接受。真实模型可以后接，第一阶段必须用 mock 跑通。

**我的处理决策：**

- 采纳“模型外围系统才是项目本体”的解释；
- 将普通聊天应用和 WebUI 从项目定义中移除；
- 把 MockLLM 写入 SPEC 的核心验收标准，而不是测试阶段的临时替身；
- 在 PLAN 中把 action parser、MockLLM、loop 排在所有真实 provider 和 demo 之前。

**对 spec/plan 的影响：** 形成了 SafeCodeLoop 的一句话目标、六维机制范围和“不使用高层 agent runner 代替核心实现”的范围外条款。

### 3.4 关键迭代二：在“全部都做”和“只做一个亮点”之间取舍

**当时的冲突：** 课程要求覆盖决策、工具、记忆、治理、反馈、配置六个维度，同时又希望项目有机制深度。如果六项平均用力，容易每项都只有接口；如果只做护栏，又可能缺少完整 harness。

**整理后的对话节选：**

> **智能体：** 你更担心“要求没覆盖”还是“没有一个机制足够深”？
> **我：** 两者都担心，但时间有限。我不想写一堆只有名字的模块。
> **智能体：** 可以用 T-shaped 方案：六维都实现可运行最小闭环，再选一条跨阶段控制链做深。治理和反馈都容易被写成提示词，也最适合用 mock 做反例。要不要把二者组合成主贡献？
> **我：** 可以，但必须能展示它们不是两个孤立功能。
> **智能体：** 那主贡献可以表述为“执行前由 guardrail 决定动作资格，执行后由 validation 决定结果资格”。

**我的处理决策：**

- 采纳 T-shaped 范围：六维最小实现，治理和反馈纵向做深；
- 修正 AI 最初把二者当并列 feature 的表达，要求它们构成同一条控制链；
- 否决多 agent、浏览器工具、IDE 插件和向量数据库等扩展；
- PLAN 按 parser/loop → tools/guardrail → feedback/memory/config → CLI/demo 排序。

**对 spec/plan 的影响：** SPEC 明确主要贡献及深度目标；PLAN 的每个阶段都有前置依赖和单独验收命令，避免多个机制一次性混写。

### 3.5 关键迭代三：老师补充说明后推翻 WebUI 计划

**新信息：** 老师在群里说明 A 类项目可以选择 CLI-only + release，不强制 WebUI。

**整理后的对话节选：**

> **我：** 老师说 Agent Harness 可以只交 CLI 和 release，这会不会推翻之前的任务？
> **智能体：** 会改变交付范围，但不会改变 harness 内核。现在有两条路线：保留 WebUI 获得展示性，或把 WebUI 降为可选，把时间转移到机制、测试和分发。你更看重哪种风险？
> **我：** 我更怕内核浅、测试和 release 做不完整。
> **智能体：** 那么建议选择 CLI-only，并同步修改 SPEC、PLAN 和 submission 元数据，不能只在实现时偷偷不做 WebUI。
> **我：** 采纳。WebUI 明确写入范围外/P1，不留下模糊承诺。

**我的处理决策：**

- 推翻第一版“WebUI 必做”的 AI 结论；
- 将 CLI-only 作为主动的资源配置选择，而不是延期借口；
- 要求省下的工作量用于三个可自动断言的 demo、真实 release、CI 和 Docker 验证；
- 明确 `submission.jsonc` 中 `is_deployed=false`，release URL 与压缩包位置保持一致。

**对 spec/plan 的影响：** SPEC 的交付章节和范围发生改写，PLAN 删除 P0 WebUI 任务并加强 CLI、Docker、release 和 README 验收。

### 3.6 关键迭代四：冷启动 agent 迫使计划从“能看懂”变为“能执行”

**验证设置：** 我把 SPEC 和 PLAN 单独交给 Gemini，不给前序对话，并要求遇到不明确就停止提问。

**整理后的对话节选：**

> **Gemini：** T2.1/T2.2 可以开始，但有四个会导致不同实现的问题：构建后端是什么？版本号从哪里读取？是否注册 console script？是否支持 `python -m safecodeloop`？
> **我：** 这些问题在我的脑中有默认答案，但文档里确实没有。请不要自行选择，我来统一口径。
> **原智能体：** 建议锁定 setuptools、单一 `__version__`、`safecodeloop` console script 和 `__main__.py`，并把四条都变成测试。
> **我：** 采纳，但额外要求三种入口共享同一个 `main()`，避免出现多个 CLI 实现。

**我的处理决策：**

- 接受冷启动 agent 的质疑，不把它归因于“agent 不够聪明”；
- 修订 SPEC 技术选型和 PLAN 文件清单；
- 把文档歧义转成 help/version/entry-point 测试；
- 建立新规则：后续子任务若仍需要实现者猜测接口来源，就先修 PLAN，不直接编码。

**对 spec/plan 的影响：** 构建、版本、命令名和模块入口全部锁定，项目骨架第一次达到可独立委派的粒度。

### 3.7 关键迭代五：实现反例反向修改原 spec

前三轮主要发生在编码前；第五轮说明 brainstorming 并未在 SPEC 初稿完成后停止。后期我把已经通过 demo 的系统重新交给智能体做反例审查。

**整理后的对话节选：**

> **我：** 现在 demo 已经能先失败再修正，这是否足以证明反馈闭环？
> **智能体：** 还不够。三个问题：`git status` 成功会不会也被算成 validation pass？失败后模型直接 `finish` 会怎样？相同失败无限重复会怎样？
> **我：** 第一个会混淆普通工具结果，第二个可能假成功，第三个会浪费步数。请先用测试证明，不要直接改实现。
> **智能体：** 失败测试确认了三处缺口。建议新增显式 `run_validation`、写入后的完成门槛、验证预算和重复失败熔断。
> **我：** 采纳，并补充检查 approval resume 是否复用同一套规则。

**我的处理决策：**

- 采纳反例而不是维护“72 tests passed 已经完成”的结论；
- 将原本宽泛的 feedback 条款拆成 observation/validation 分离、有界证据、完成门槛、预算和熔断；
- 发现 resume 旁路后要求先写红灯测试，再统一状态机；
- 同步回写 SPEC 和 PLAN，避免代码能力领先于设计文档。

**对 spec/plan 的影响：** 形成 T12.1—T12.3，最终把“能反馈”提升为“反馈有权拒绝完成”。

### 3.8 五轮迭代的闭环摘要

| 轮次 | 初始假设 | 智能体提出的关键问题 | 我的决定 | 文档结果 |
|---|---|---|---|---|
| 1 | 做 AI 编程应用 | 项目本体是模型还是 harness？ | 选择自研 harness | 冻结六维范围 |
| 2 | 六项尽量都做多 | 广度与深度如何兼顾？ | T-shaped，治理 + 反馈做深 | 确立主贡献和阶段顺序 |
| 3 | WebUI 必做 | 老师澄清后如何重新分配风险？ | CLI-only + release | 改写交付范围 |
| 4 | SPEC/PLAN 已足够清楚 | 新 agent 还需要猜什么？ | 锁定 packaging 与入口 | 文档达到可委派粒度 |
| 5 | demo 通过即闭环完成 | 普通命令、finish、重复失败能否绕过？ | 接受反例并增加 T12 | 强化反馈不变量 |

## 4. 初始问题：从课程文字中识别真正要做的系统

### 4.1 第一版理解为什么不够

最初面对的要求混合了通用软件工程交付、A 类 Agent Harness 特殊要求、WebUI/部署、凭据、过程文档和机制演示。如果逐条堆功能，项目很容易变成一个带聊天页面的普通应用，却没有真正实现 agent harness。

第一轮分析先把问题重新表述为：

> 课程要评估的不是模型本身，而是模型外围能否做出可控制、可观察、可测试、可分发的工程系统。

因此项目必须回答六个机制问题：

| 维度 | 必须回答的问题 | 最初采用的最小机制 |
|---|---|---|
| 决策 | 模型输出如何变成可执行动作 | JSON action schema + parser + agent loop |
| 工具 | 动作如何安全地接触真实环境 | registry + 结构化 `ToolResult` |
| 记忆 | 历史信息如何进入下一轮且不泄密 | 持久化 memory + 相关性/优先级选择 |
| 治理 | 谁决定动作能否执行 | 代码化 guardrail：allow/block/needs_approval |
| 反馈 | 什么客观信号能迫使 agent 修正 | validator + 分类 feedback + observation 回灌 |
| 配置 | 如何改变行为而不修改代码 | 有默认值、校验和运行时效果的 JSON config |

### 4.2 项目定位的人工收敛

AI 最初能列出多 agent、浏览器、IDE 插件、WebUI、向量记忆等大量可能功能。项目负责人没有按“功能越多越好”的方向扩展，而是设定两个选择标准：

1. 是否能用 MockLLM 稳定复现；
2. 是否能直接证明“harness 不是 prompt wrapper”。

据此，主贡献被锁定为：

> 执行前治理护栏 + 执行后测试反馈闭环。

这不是两个并列卖点，而是一条完整控制链：guardrail 决定“动作是否有资格发生”，feedback 决定“发生后的结果是否足以完成任务”。前者控制副作用，后者控制假成功。

### 4.3 范围冻结

人工最终做出的范围决策是：六个维度都有可运行的最小实现，但深度集中在治理和反馈；不做多 agent 调度、不做 IDE 插件、不做大型 WebUI、不使用高层 agent runner 代替核心循环。

这个取舍随后写入 `SPEC.md` 的“范围内/范围外”和 `PLAN.md` 的阶段任务，成为后续所有 AI 编码任务的边界。

## 5. 关键外部信息导致的第一次规约改写

### 5.1 冲突来源

通用交付要求提到线上部署 URL 和 WebUI，因此第一版任务拆分曾把 WebUI 视为核心交付物。之后老师补充说明：A 类 Agent Harness 可以采用以下任一路线：

- CLI-only + 托管平台 release 链接；
- CLI + WebUI + WebUI 部署链接。

这条信息改变的不是一个小功能，而是整个风险分配。

### 5.2 方案比较

| 方案 | 优点 | 成本与风险 | 最终判断 |
|---|---|---|---|
| CLI + WebUI | 展示直观 | 引入前端、部署、跨平台路径和额外测试；稀释机制深度 | 否决为 P0 |
| CLI-only + release | 更接近 harness 本体；易于自动测试和打包 | 展示性较弱，需要更好的 demo 和 README | 采用 |

项目负责人选择 CLI-only，不是因为“做不出 UI”，而是因为评分核心是 agent 主循环、机制深度、TDD 证据和可复现分发。节省的范围后来投入了可恢复审批、反馈压缩、验证预算和 Docker 干净环境验证，这些增强比一个薄 WebUI 更能证明工程能力。

### 5.3 对文档和交付的连锁影响

- `SPEC.md` 将 WebUI 从范围内必做改为可选增强；
- `PLAN.md` 锁定 Python CLI、Docker 和 release 路线；
- `submission.jsonc` 采用 `is_deployed=false`，部署字段填写 release 链接；
- README 和 demo 成为评审者复现机制的主要入口；
- 后续测试优先验证 CLI 输出、运行日志和包安装，而不是界面交互。

这次修订证明：规约必须能够吸收权威澄清，不能为了维护第一版文档的一致性而坚持错误范围。

## 6. 冷启动验证：用另一个 agent 检查规约是否可执行

### 6.1 验证设计

在核心实现开始前，项目使用 Gemini 做了一次隔离的冷启动验证。它只获得 `SPEC.md` 和 `PLAN.md`，没有获得先前对话，提示要求是：

```text
你只能看到 SafeCodeLoop 的 SPEC.md 和 PLAN.md，不能使用任何先前对话上下文。
请从 PLAN.md 中选择 1—2 个早期实现任务尝试推进。
如果任何需求、文件路径、行为或测试期望不明确，请停止并提问，不要猜测。
请记录你被什么卡住，以及你倾向于做出哪些假设。
```

该验证刻意不要求另一个 agent 大量写代码，而是测试两个文档能否让一个“失忆的新开发者”做出一致实现。

### 6.2 暴露的四个歧义

Gemini 正确选择 T2.1 项目骨架和 T2.2 CLI 入口，但指出：

1. `pyproject.toml` 没有锁定构建后端；
2. CLI version 测试没有规定版本号的唯一来源；
3. 没有说明是否注册全局命令及命令名；
4. 只写了 `python -m safecodeloop.cli`，没有说明是否支持 `python -m safecodeloop`。

这些不是实现者能力问题，而是规约可执行性不足。若让多个 agent 并行实现，它们可能分别选择 Poetry、setuptools、不同入口或重复维护版本号，最终造成无意义冲突。

### 6.3 修订结果

- 构建后端锁定为 `setuptools`；
- 包名和 CLI 命令名锁定为 `safecodeloop`；
- 版本号唯一来源为 `src/safecodeloop/__init__.py` 的 `__version__`；
- 增加 `src/safecodeloop/__main__.py`；
- `[project.scripts]` 固定为 `safecodeloop = "safecodeloop.cli:main"`；
- 测试同时覆盖全局入口、模块入口、help 和 version。

### 6.4 对方法的认识

冷启动验证的价值不是“第二个 AI 也认可方案”，而是寻找它必须猜测的地方。只要它需要猜测，SPEC/PLAN 就还没有达到可委派粒度。这一标准后来继续用于审批恢复、验证动作和完成门槛的复审。

## 7. 第一阶段演化：先建立可确定性测试的最小内核

### 7.1 为什么先做 action 和 MockLLM

如果一开始接真实模型，任何失败都可能来自网络、模型随机性、提示词、解析器或工具，无法判断 harness 本身是否正确。因此计划把真实 provider 延后，先完成：

- action schema 和严格 parser；
- 按脚本返回响应的 MockLLM；
- 有最大步数和明确终态的 agent loop。

对应提交为 `85c0088`、`eb6ada6`、`7e11986`，全量测试从 4 增长到 19。

### 7.2 TDD 如何改变接口

Action Parser 不是“能解析 JSON 即可”。失败测试迫使它区分：非法 JSON、非对象顶层、未知 action、缺少 arguments、finish 缺少 message。人工选择让 parser 只负责语法和 schema，不负责执行与治理，从而保持职责边界。

MockLLM 也不只是一个返回字符串的假对象。它需要：

- 按顺序消费脚本；
- 记录接收到的上下文，便于断言反馈是否真的回灌；
- 脚本耗尽时明确失败；
- 对日志中的敏感内容进行脱敏。

主循环第一版只支持 parse error、finish 和 max_steps。项目负责人刻意没有提前把所有工具、护栏和记忆揉进循环，以便后续每接入一个机制，都能用测试准确定位契约变化。

### 7.3 本阶段形成的规约原则

> 模型响应不是指令，只是未经信任的候选 action；只有 parser、guardrail 和 tool dispatcher 共同接受后，动作才可能产生副作用。

这条原则后来成为审批签名、显式验证和完成门槛的基础。

## 8. 第二阶段演化：把“能调用工具”改造成“副作用受治理”

### 8.1 工具返回结构化结果

工具系统先实现 registry，再分别实现文件工具和命令工具。人工做出的关键判断是：命令非 0 退出码不应抛出 Python 异常，而应返回包含 exit code、stdout、stderr 和错误状态的 `ToolResult`。

原因是异常只告诉循环“程序出错了”，结构化结果才能成为后续反馈分类器的输入。这个决定把工具层与反馈层连接起来，同时又避免工具层提前理解 pytest、lint 或 typecheck 语义。

### 8.2 路径安全不是字符串前缀判断

文件工具的风险不只是明显的绝对路径，还包括 `..`、路径规范化和工作区边界。规约因此要求先解析并规范化路径，再判断是否仍位于 workspace 内。测试直接验证越界读取和写入被拒绝。

### 8.3 Guardrail 的三态语义

第一版 AI 建议可以简单采用 allowlist/blocklist。人工没有把所有风险动作一律 block，而是区分：

- `allow`：可以立即执行；
- `block`：无合理使用场景或风险不可接受，例如递归删除根目录、敏感文件访问；
- `needs_approval`：动作可能合理但有外部副作用，例如安装依赖。

这个三态设计比二元拦截更接近真实 coding agent：治理不是让 agent 什么都做不了，而是让不同风险进入不同控制路径。

### 8.4 执行顺序成为可测试不变量

在 `b683212` 串联 Loop、Guardrails 和 Tools 时，验收标准被写成顺序约束：

```text
parse action → guardrail decision → tool dispatch
```

测试不仅断言最终状态为 `blocked`，还断言 executor 没有收到危险动作。这样可以防止一种“表面拦截”：命令已经执行，系统才在日志中说它被阻止。

全量测试由 19 增长到 41。此时治理机制已经能确定性运行，但 `needs_approval` 仍只是暂停点，尚不是完整人机协作状态机；这一缺口在后期对抗性复审中被重新打开。

## 9. 第三阶段演化：从原始命令输出到反馈闭环

### 9.1 初版反馈假设

反馈层最初采用 Validator + Feedback Classifier：根据退出码和输出将结果分为 pass、test_failure、syntax_error、timeout、environment_error 等类别，再通过 `Feedback.to_observation()` 进入下一轮上下文。

`tests/test_feedback_loop.py` 使用 MockLLM 构造了确定性链条：第一次测试失败，下一轮上下文出现 `feedback_kind: test_failure`，模型脚本随后写入修正，第二次测试通过。

### 9.2 为什么不能只回灌 stdout/stderr

AI 最初容易选择把命令输出原样拼回 prompt。这种做法有三个问题：

1. 输出可能极长，挤占上下文；
2. 同一类错误在不同平台输出不同，不利于稳定测试；
3. LLM 很难区分“命令执行失败”“测试断言失败”和“环境缺依赖”。

人工决定把“分类摘要”和“完整证据”分开：模型接收稳定的结构化 feedback，运行日志保存原始结果。后期又加入字符上限、SHA-256 和日志引用，使该原则完整落地。

### 9.3 记忆与上下文不是无限追加

Memory Store 的设计也经历了范围控制。没有采用向量数据库，而是保存可读的结构化项目事实、决策和失败模式，并按优先级、相关性与条数限制选择上下文。疑似密钥不直接原样保存，而是脱敏后保留事件语义。

这一选择体现了课程项目的规模判断：目标是证明 context engineering 的机制存在，不是借复杂基础设施制造表面规模。

### 9.4 配置必须改变真实行为

配置加载器不仅反序列化 JSON，还校验类型、范围和未知字段，并通过可配置 blocked pattern 改变 guardrail 结果。对应测试证明 config 是运行控制面，而不是一个从未被消费的样例文件。

本阶段全量测试从 41 增长到 66，覆盖反馈、记忆、上下文、配置和第一版凭据命令。

## 10. 第四阶段演化：将机制变成评审者可以复现的证据

### 10.1 CLI 不是简单包装

`run` 子命令统一加载 config、workspace、MockLLM script、日志路径和任务文本，并将最终状态映射为可理解的输出。CLI 的职责是提供可复现实验入口，而不是重新实现 loop 逻辑。

### 10.2 三个 demo 的证据设计

| Demo | 要证明的命题 | 关键可观察证据 |
|---|---|---|
| 危险动作拦截 | 治理发生在执行前 | `status: blocked`，有规则原因，命令未进入工具层 |
| 失败后修正 | 客观反馈能改变下一步动作 | 先 `test_failure`，再写修正，再 `pass`，最终 success |
| 主贡献组合 | 治理与反馈属于同一控制链 | 先完成失败—修正—通过，再尝试危险动作并被 block |

第三个 demo 最初只计划“同时出现两个功能”。人工将验收标准加强为有因果顺序的组合场景。由于 `blocked` 和 `needs_approval` 都是立即终止状态，危险动作必须放在反馈闭环之后；否则演示会在展示修正能力之前结束。

这是一处典型的“实现约束反向澄清规约”：不是 demo 文件怎么排步骤的问题，而是系统终态语义决定了证据叙事的顺序。

### 10.3 避免脆弱演示

反馈修正 demo 曾出现第二次 pytest 可能复用旧 `.pyc` 的边界问题。修正版文件加入内容差异，避免极短时间内时间戳和缓存行为使演示误判。这类修正虽然不改变功能，但提高了评审环境中的确定性。

阶段结束时，本地全量测试达到 72 项，三个 demo 均由 pytest 自动断言，而不是只靠录屏或人工说明。

## 11. 第五阶段演化：凭据、真实模型和完整人机审批

### 11.1 凭据方案从“可用”修正为“安全失败”

第一版凭据功能使用隔离 JSON backend，能完成 set/status/clear 且不回显 key，但生产路径仍存在明文落盘风险。项目负责人没有删除早期记录来制造“一开始就设计正确”的假象，而是在后续安全复审中明确修订：

- 生产 CLI 使用 OS keyring；
- `key set` 使用隐藏输入，不接受命令行明文参数；
- 测试可以显式注入临时文件 backend；
- keyring 不可用时明确失败，不静默降级为明文文件。

这里否决了 AI 常见的“为了可用性自动 fallback”建议。对凭据而言，静默降低安全等级比明确失败更危险。

### 11.2 真实 provider 只替换决策组件

OpenAI-compatible adapter 被作为独立增强加入。人工坚持架构边界：真实 provider 只负责单次 `/chat/completions` 决策，主循环、action parser、工具、guardrail、feedback 和停止规则仍由 SafeCodeLoop 自己实现。

这避免了两个问题：

- 使用外部 agent framework 代替课程要求实现的 harness；
- 把真实模型偶然成功误当成机制正确。

真实调用只做最小 smoke test，日常回归仍全部依赖 MockLLM。这样网络和额度不会成为评分者验证核心机制的前置条件。

### 11.3 `needs_approval` 暴露出的语义空洞

后期复审发现：第一版系统能返回 `needs_approval`，却没有“人批准后如何继续”。这意味着它只有风险标签，没有完整 Human-in-the-Loop 流程。

项目因此新增可恢复审批状态机：

```text
风险 action
   ↓
创建 pending 记录并暂停
   ↓
独立 CLI 进程 inspect / approve / reject
   ↓
resume 时校验记录与 action 绑定关系
   ↓
在工具调用前一次性消费 approval
   ↓
执行或安全失败
```

### 11.4 审批状态机的人工安全决策

- 选择“一次性批准”，否决对同类命令永久放行；
- approval 与 canonical action 绑定，避免替换参数后复用；
- 使用 HMAC-SHA256 检测记录篡改，签名 key 单独存入 OS keyring；
- 在工具调用前消费批准，即使工具失败也不能重放；
- rejected、未批准、已消费、动作不匹配或记录损坏全部 fail closed；
- 不持久化完整对话，只保存恢复所需最小状态。

测试曾暴露“先比较动作，再检查存储完整性”会给出错误分类，顺序被修正为先验证记录完整性，再比较调用动作。最终审批相关测试达到 15 项，全量回归达到 97 项。

## 12. 第六阶段演化：对反馈闭环做对抗性复审

到 97 项测试时，系统已经“能跑、能演示、能审批”。但项目负责人没有把 demo 通过等同于机制完备，而是从攻击者和误用模型的角度提出三个问题：

1. 普通命令是否会被错误声称为客观验证？
2. 超长输出或不同失败类型是否会破坏上下文？
3. 模型能否在失败后直接 `finish`，制造假成功？

这三问形成 T12.1—T12.3。

### 12.1 T12.1：普通命令与客观验证分离

初版实现把所有 `run_command` 结果交给 Validator，因此 `git status` 成功也可能被描述为“验证通过”。这在类型层混淆了工具 observation 和质量证据。

修订方案是新增显式 `run_validation` action：

- `run_command` 只返回工具结果；
- `run_validation` 才产生 feedback；
- 验证命令仍经过相同 guardrail，不获得旁路权限。

失败测试一次暴露 parser、tool registry 和 loop 三处缺口。修订后全量测试达到 100 项。

### 12.2 T12.2：失败分类与有界反馈

复审构造了 syntax error、type error、lint failure、environment error 和超长输出等反例。第一轮相关测试出现 `6 failed, 7 passed`，证明原规约对分类与上下文预算描述不足。

最终机制为：

- 完整 stdout/stderr 保存在 run log；
- 模型默认只接收最多 1200 字符的诊断详情；
- 摘要优先保留失败位置、错误码、异常和断言行；
- observation 携带原始字符数、SHA-256 和证据位置；
- 被截断内容仍可审计，而不会挤爆上下文。

这一步把“context management”从 memory 条数控制扩展到动态工具证据控制。全量回归达到 106 项。

### 12.3 T12.3：完成门槛、预算与熔断器

最严重的反例是：模型在测试失败后直接输出 `finish`，旧循环会接受成功；代码写入后不重新验证也可能完成。这说明有反馈不等于反馈具有约束力。

项目新增三个状态不变量：

1. 成功写入代码或工程配置后，设置“需要新的客观验证”；
2. 验证失败后，`finish` 产生 `completion_rejected` observation，循环继续；
3. 只有更新后的 validation pass 才能解除完成门槛。

同时增加两种终止控制：

- `maxValidations`：限制总验证次数，防止无限消耗；
- `maxRepeatedFailures`：相同类别和摘要连续失败时打开熔断器。

代码复审又发现审批恢复路径可以绕过完成门槛。项目先补红灯测试，再让 `resume()` 复用相同的验证状态机、预算和熔断规则，而不是复制一套较弱逻辑。全量测试达到 114 项。

这轮演化使主贡献从“能看到反馈”提升为“反馈能决定任务是否允许成功”。

## 13. 第七阶段演化：分发环境作为新的规约验证器

### 13.1 为什么 Docker 不是打包附件

Dockerfile 最初因为本机未安装 Docker 而只能静态检查；安装后又因 Docker Hub 网络不可达无法拉取基础镜像。过程日志把验证拆成四层：

1. Docker CLI 是否可用；
2. registry 和基础镜像是否可达；
3. image 是否能够 build；
4. 容器中的 CLI、测试 demo 和文件边界是否正确。

这避免了把外部网络失败误写成项目失败，也避免了仅有 Dockerfile 就声称容器已验证。

### 13.2 干净环境暴露的两个本地盲点

代理修正后，镜像首次构建成功，但容器内 demo 暴露两个问题：

**问题一：安全任务过早初始化 OS keyring。**
本地机器有可用凭据服务，所以此前测试没有显示问题；容器中的普通 MockLLM 任务却不应依赖 keyring。修订后审批存储惰性初始化：安全任务不访问凭据系统，只有真正创建或恢复审批时才要求 keyring。随后增加风险动作回归，确认惰性初始化没有削弱审批安全。

**问题二：镜像缺少 pytest。**
CLI 本身可运行，但反馈 demo 的验证命令在容器中分类为 environment_error，并因重复失败触发熔断。项目将 pytest 纳入演示镜像，使 release image 可以无额外安装复现核心闭环。

### 13.3 最终容器证据

- Docker Desktop Engine 29.7.2 成功构建 `safecodeloop:0.1.0`；
- 容器内 `--help`、`--version` 通过；
- 完整 failure—correction—pass MockLLM demo 通过；
- 镜像不包含 `.git`、release 产物、本地审批目录和内部执行计划；
- 全量测试最终达到 `116 passed`。

Docker 因此充当了一次环境独立性测试，并再次反向推动运行时依赖和惰性初始化设计。

## 14. 规约追踪矩阵

下表说明最终 SPEC 的主要条款如何被过程证据覆盖。

| SPEC 机制 | 实现位置 | 关键测试/演示 | 形成该条款的过程原因 |
|---|---|---|---|
| Action schema | `actions.py` | `test_actions.py` | 防止自由文本直接变成副作用 |
| MockLLM | `llm.py` | `test_llm.py` | 隔离真实模型随机性和网络 |
| Agent loop | `loop.py` | `test_loop.py` | 明确 observation、终态和 max_steps |
| Tool registry | `tools.py` | `test_tools.py` | 解耦 action 与执行器 |
| Workspace 文件边界 | `tools.py` | `test_file_tools.py` | 阻止路径规范化后的越界访问 |
| 结构化命令结果 | `tools.py` | `test_command_tool.py` | 让失败可被反馈层消费 |
| 三态 guardrail | `guardrails.py` | `test_guardrails.py` | 区分不可接受风险与需人确认风险 |
| 执行前拦截 | `loop.py` | `test_loop_tools_guardrails.py`、危险动作 demo | 防止“执行后才记录拦截” |
| 显式 validation | `actions.py`、`tools.py`、`loop.py` | `test_feedback_loop.py` | 区分普通 observation 与客观质量证据 |
| Feedback 分类 | `feedback.py` | `test_feedback.py` | 用稳定类别代替不可控原始文本 |
| 有界反馈与证据引用 | `feedback.py`、`loop.py` | 分类/长输出测试 | 平衡上下文预算与可审计性 |
| 完成门槛 | `loop.py` | `test_loop.py`、`test_approval.py` | 阻止失败后或写入后直接假成功 |
| 验证预算/熔断 | `loop.py`、`config.py` | feedback loop/config 测试 | 防止无限验证和重复失败循环 |
| 可恢复审批 | `approval.py`、`cli.py` | `test_approval.py` | 把 needs_approval 从标签补成 HITL 流程 |
| OS 凭据库 | `credentials.py` | `test_credentials.py` | 否决生产明文 fallback |
| Memory/context | `memory.py`、`loop.py` | memory/context 测试 | 控制持久事实、相关性和泄密 |
| Config 控制面 | `config.py` | `test_config.py` | 保证配置真实改变机制行为 |
| 可复现分发 | Dockerfile、CI、release script | CI、wheel smoke、容器 demo | 证明不是只在开发目录可运行 |

## 15. 关键决策登记表

| 决策 | 曾考虑或出现的替代方案 | 人工最终判断 | 可观察影响 |
|---|---|---|---|
| CLI-only | WebUI 作为 P0 | 采用老师允许的 release 路线，把时间投入机制 | README、demo、Docker 成为主要交付入口 |
| MockLLM 优先 | 先接真实模型 | 确定性优先，真实调用只做增强 smoke | 核心测试无需网络和 key |
| 自研 loop | 使用高层 agent runner | 课程要求的机制必须在仓库中可见 | parser/loop/tools/guardrail 均独立实现 |
| 三态治理 | 所有风险一律 block | 合理风险进入 approval | 后续发展为可恢复 HITL 状态机 |
| 结构化失败 | 非 0 退出码抛异常 | 把失败当 observation | feedback classifier 可统一处理 |
| 显式 validation | 所有命令都算验证 | 协议层区分工具结果与质量证据 | `run_validation` action |
| 有界上下文 | 原始输出全量回灌 | 摘要 + hash + 日志引用 | 长输出不会吞噬 context |
| 完成由状态机控制 | 相信模型的 finish | 写入/失败后必须重新验证 | completion_rejected、预算、熔断 |
| OS keyring | 自动明文 fallback | 安全失败优先 | 生产 CLI 不静默降级 |
| 一次性审批 | 对命令类型永久放行 | approval 绑定 canonical action 且一次消费 | 防重放、防换参、防篡改 |
| Git 跟踪文件打包 | 手工选择文件压缩 | 以 `git ls-files` 为 release 来源 | 降低密钥、日志和临时文件混入风险 |

## 16. AI 贡献、人工贡献与被否决建议

### 16.1 AI 适合承担的工作

- 将课程长要求拆成六个 harness 维度；
- 提出模块边界、测试用例和初版接口；
- 按 task 编号生成失败测试与最小实现；
- 分析测试输出、定位回归原因；
- 生成 demo script、CI、Docker 和文档草稿；
- 在代码复审中寻找完成门槛、审批恢复等旁路。

### 16.2 项目负责人保留的判断

- 选择项目方向和主要贡献；
- 根据老师说明修改交付路线；
- 决定哪些功能不做，防止范围膨胀；
- 规定 guardrail、approval 和 credential 的安全等级；
- 判断测试是验证了机制，还是只验证了表面输出；
- 要求 demo 具有因果顺序；
- 决定真实 provider 的架构边界；
- 对 Docker、网络和 release 状态做诚实区分；
- 在合并前运行全量回归并检查文档是否与实现一致。

### 16.3 被修改或否决的典型 AI 建议

1. **“WebUI 是通用要求，所以必须做。”**
   根据老师补充说明改为可选，选择 CLI-only + release。

2. **“在系统提示词中要求模型不要运行危险命令。”**
   否决。提示词可以说明规则，但真正拦截必须发生在工具执行前的确定性代码中。

3. **“命令失败直接抛异常即可。”**
   修改为结构化 `ToolResult`，使失败能进入反馈闭环。

4. **“所有命令执行后都交给 Validator。”**
   后期反例证明这会把 `git status` 错当验证，改为显式 `run_validation`。

5. **“keyring 不可用时自动写本地 JSON，提升兼容性。”**
   否决生产降级，只允许测试显式注入文件 backend。

6. **“needs_approval 返回状态就算完成人工治理。”**
   修改为可持久化、可恢复、防篡改、一次性消费的审批状态机。

7. **“模型输出 finish 就结束。”**
   修改为由 harness 的验证状态机裁决完成条件。

8. **“有 Dockerfile 即可说明支持容器。”**
   否决。最终要求实际 build、run、demo 和镜像内容检查。

## 17. 过程量化证据

### 17.1 测试增长

| 里程碑 | 全量测试结果 | 主要新增能力 |
|---|---:|---|
| 包结构与 CLI | 4 passed | import、help、version |
| Action + MockLLM + Loop | 19 passed | 最小决策循环 |
| Tools + Guardrail | 41 passed | 受控副作用 |
| Feedback + Memory + Config + Credential v1 | 66 passed | 反馈与运行控制面 |
| CLI + 三个 demo | 72 passed | 可复现机制证据 |
| OS keyring + provider | 87 passed | 安全凭据与真实决策适配 |
| 可恢复审批 | 97 passed | 完整 HITL |
| 显式验证 | 100 passed | observation/validation 分离 |
| 有界反馈 | 106 passed | 分类、压缩和证据引用 |
| 完成门槛与熔断 | 114 passed | 防假成功与无限循环 |
| Docker 干净环境修正 | 116 passed | 惰性 keyring、容器内 demo |

测试数量不是质量本身，但增长轨迹能说明机制是分阶段加入并持续回归，而不是最后一次性补测试。

### 17.2 版本与协作证据

项目保留了从 `4f7548f` 初始化规约与 CLI，到 `5418cc2` 准备可复现 release 的连续提交。增强阶段使用独立分支和 PR，包括安全凭据、GitHub Actions、OpenAI-compatible provider、可恢复审批，以及三组反馈深度增强。

重要的是：提交不是按文件类型机械拆分，而是按可验证机制拆分。例如 `22475a0` 只处理显式验证语义，`007643c` 只处理分类和有界反馈，`20fde80` 只处理完成门槛与停止控制。这使设计变化能够被独立审查和回滚。

## 18. 风险账本及处置

| 风险 | 早期判断 | 实际暴露方式 | 处置结果 |
|---|---|---|---|
| 范围膨胀 | 高 | WebUI、多 agent 等建议不断出现 | 冻结 CLI-only 和范围外清单 |
| 真实模型不可复现 | 高 | 网络、额度、响应随机性 | MockLLM 为主，真实 provider 只 smoke |
| 危险动作绕过 | 高 | 执行顺序与 validation 旁路测试 | 统一执行前 guardrail |
| 假成功 | 初期低估 | 失败后直接 finish 的反例 | 完成门槛、预算、熔断 |
| 审批重放/篡改 | 中 | HITL 状态机威胁建模 | action 绑定、HMAC、一次消费 |
| 凭据明文泄露 | 高 | 第一版 JSON backend 复审 | OS keyring、隐藏输入、无生产 fallback |
| 上下文膨胀 | 中 | 超长验证输出测试 | 1200 字符摘要 + evidence reference |
| 容器环境差异 | 中 | keyring 初始化、缺 pytest | 惰性初始化、补齐演示依赖 |
| release 混入本地文件 | 中 | 手工压缩不可审计 | `git ls-files` 打包 + 排除检查 |
| 文档与现实不一致 | 高 | Docker 网络状态、后期机制增强 | 保留历史记录，新增修正说明 |

## 19. 当前仍然承认的边界

即使最终达到 116 项测试并完成容器验证，SafeCodeLoop 仍不是生产级安全沙箱：

- 允许的命令最终仍在宿主 shell 或容器中执行；guardrail 是治理层，不等价于 OS 级隔离；
- 规则匹配无法证明覆盖所有恶意 shell 表达形式；
- OpenAI-compatible adapter 只做了最小真实 smoke，不保证所有供应商的错误格式一致；
- OS keyring 的可用性依赖目标系统；无可用 backend 时风险动作审批会安全失败；
- memory 是轻量结构化存储，不提供大规模语义检索；
- 单 agent loop 适合小型演示任务，不面向长周期、多仓库自主开发；
- WebUI 未实现，这是主动范围选择，不是隐藏完成项。

把这些限制写入文档不是削弱项目，而是明确机制的适用边界，避免把教学 harness 宣称为生产级自治系统。

## 20. 对 Superpowers brainstorming 技能的反思

### 20.1 做得好的地方

**第一，它把“我想做什么”变成了“评审者如何证伪我”。**
最有价值的追问不是技术栈偏好，而是“移除真实 LLM 后还能证明什么”“危险动作是否真的没有进入 executor”“失败后为什么不能直接 finish”。这些问题直接生成了验收标准和失败测试。

**第二，它迫使我在编码前表达取舍。**
brainstorming 给出多个方案并要求比较，使我明确说明为什么选择 CLI-only、为什么六维最小覆盖但治理/反馈做深、为什么生产凭据不能自动明文 fallback。文档因此记录了理由，而不只有结论。

**第三，它擅长发现隐含默认值。**
构建后端、版本号来源、入口命令、approval 的消费时机等问题，对项目作者来说“似乎理所当然”，对新 agent 却并不明确。冷启动检查证明这些追问能实质降低后续实现分歧。

**第四，它让 SPEC 和 PLAN 保持联动。**
一个设计决定不仅改 SPEC 描述，还会落到 PLAN 的任务、测试和验证命令。例如交付路线变化同时影响 WebUI 范围、Docker、release、README 和 submission 元数据；显式 validation 同时影响 action、tool、loop、demo 和 config。

**第五，它能在已有成功证据后继续提出反例。**
当 72 项测试和三个 demo 已通过时，brainstorming 没有只做总结，而是追问普通命令、假成功、无限验证和恢复旁路。这类“对成功结果不满足”的能力最终形成了项目最有深度的部分。

### 20.2 让我不满的地方

**第一，早期建议容易发散。**
如果不主动强调课程类型、时间和评分证据，brainstorming 会列出多 agent、WebUI、向量数据库、IDE 集成等看起来高级但不必要的功能。它擅长扩大可能性，却不天然替我承担范围成本。

**第二，对外部课程语境没有可靠知识。**
在我提供老师群里的补充说明前，它根据通用要求把 WebUI 当作必做。说明它只能基于已有上下文推理，不能代替我确认课程最新口径。

**第三，有时把“合理建议”说得像“确定要求”。**
例如技术栈、模块名和分支策略本来有多种可行解，AI 初稿会快速锁定一种。如果我不追问依据，建议可能被误写成课程要求。后来我要求所有关键项标记为“课程硬约束、工程推导或个人选择”。

**第四，容易产生形式化文档。**
brainstorming 很容易生成完整的目标、非目标、用户故事和风险清单，但这些章节如果没有对应测试或计划任务，只是格式正确。第一版过程文档就有这个问题：看似覆盖内容，实际只有总结，没有展示追问和决策冲突。

**第五，不能自动保证对话记录的证据强度。**
AI 可以把过程写得流畅，但流畅不等于真实。测试数、提交、分支、Docker 状态仍需我从仓库核对；早期聊天没有完整导出时，也必须明确标注“整理后的节选”，不能伪装成逐字原始记录。

### 20.3 我对该技能的最终评价

Superpowers brainstorming 最适合做“决策放大器”和“歧义探测器”，不适合替项目负责人做最终裁决。它提升质量的前提是：我提供真实约束、拒绝不必要范围、要求建议落到验收测试，并保留推翻 AI 结论的权力。

如果重新开始，我会在第一轮就要求它为每个建议同时给出：依据属于课程要求还是工程判断、若不做有什么影响、用什么测试验证、会增加多少范围。这样能更早过滤“听起来厉害但不增加可评分证据”的建议。

## 21. 最终反思：真正改变项目质量的不是“让 AI 多写”，而是提高反例密度

回看整个过程，最有价值的变化并不是代码从少变多，而是每一轮都增加了一个更强的反例：

- 冷启动 agent 证明文档存在 packaging 歧义；
- executor 未调用断言证明 guardrail 真的发生在执行前；
- 普通命令反例证明 feedback 语义过宽；
- 超长输出证明上下文管理不能只控制 memory；
- 失败后 finish 证明“有反馈”仍可能假成功；
- approval resume 证明主路径正确不代表恢复路径正确；
- 干净容器证明本地通过不代表分发环境完整。

因此，本项目对 AI 协作的最终认识是：

> AI 最适合快速提出候选设计和实现；人最重要的工作，是定义不能被绕过的不变量，并不断设计能够推翻当前版本的反例。

SafeCodeLoop 的 SPEC 最终从“实现一个带护栏和反馈的迷你 agent”演化为一组更精确的不变量：未经解析的模型输出不能执行，未经治理的动作不能执行，未经客观验证的修改不能完成，未经完整性校验的批准不能恢复，未经有界处理的证据不能无限进入上下文。

这条演化链才是 `SPEC_PROCESS.md` 要证明的核心：规约不是为了在实现前显得完整，而是为了让实现过程中的每一次失败都能转化为更清楚、更可测试、更难绕过的工程约束。
